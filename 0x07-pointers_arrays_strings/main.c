#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *t;
    char *o = "";
    t = _strstr("First, solve the problem. Then, write the code.", "");
    printf("%s\n%s", t, o);
    return (0);
}